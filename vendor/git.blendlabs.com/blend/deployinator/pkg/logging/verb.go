package logging

const (
	// VerbLogin is an audit verb.
	VerbLogin = "login"

	// VerbCreate is an audit verb.
	VerbCreate = "create"
	// VerbEdit is an audit verb.
	VerbEdit = "edit"
	// VerbDelete is an audit verb.
	VerbDelete = "delete"
	// VerbView is an audit verb.
	VerbView = "view"

	// VerbProvision is an audit verb.
	VerbProvision = "provision"
	// VerbDeploy is an audit verb.
	VerbDeploy = "deploy"
	// VerbDeprecate is an audit verb.
	VerbDeprecate = "deprecate"

	// VerbGrant is an audit verb.
	VerbGrant = "grant"
	// VerbRevoke is an audit verb.
	VerbRevoke = "revoke"

	// VerbGet is an audit verb.
	VerbGet = "get"

	// VerbRun is an audit verb.
	VerbRun = "run"

	// VerbExpire is an audit verb.
	VerbExpire = "expire"
)
